# Agent Working Memory (NOTES.md)

> This file persists agent context across sessions and compaction cycles.
> Updated automatically by agents. Manual edits are preserved.

## Active Sub-Goals
<!-- Current objectives being pursued -->

- **V0.7-A.3 env-aware image composition + voice anti-hallucination** — kickoff brief at `grimoires/loa/specs/build-env-aware-image-composition-v07a3.md` (combined PRD+SDD per `external_work_plan: true`). Sprint plan at `grimoires/loa/sprint-v07a3.md` (cycle-002, single sprint, 3 phases, 10 tasks). Beads epic `bd-ypi` + tasks `bd-ypi.1-bd-ypi.10`. G1 gate (imagegen path) already green via PR #15 V0.11.3. G2 gate (KEEPER pass on blocklist server) operator-bounded post-merge. Bundles §1-§10 (image composition: composeWithImage + bytes-via-webhook-attach) + §11 (anti-hallucination: persona instructions + grail-ref-guard validator) per kickoff §11 "don't bifurcate". Next: implementation via Agent dispatch + bridgebuilder review + merge.
- **V0.7-A.1 environment substrate** — cycle-001 closed. Phase A-E shipped (Phase A via commit `daed064`; Phases B-E via PRs #6-#19). Only Sprint 5 OPTIONAL webhook density experiment remains open as bd-3i7 epic + bd-19z/bd-2fp/bd-1z1/bd-3jf tasks. Operator-paced; not blocking V0.7-A.3.

## Discovered Technical Debt
<!-- Issues found during implementation that need future attention -->

- **`docs/` ↔ `grimoires/loa/specs/legacy-imports/` duplication** — mount PR copied AGENTS.md, ARCHITECTURE.md, CIVIC-LAYER.md, MULTI-REGISTER.md, CHARACTER-AUTHORING.md into legacy-imports/ without removing originals. Follow-up cleanup: pick canonical home (likely grimoires/), rewrite intra-repo references, delete duplicates. Track as separate PR.

## Blockers & Dependencies
<!-- External factors affecting progress -->

## Session Continuity
<!-- Key context to restore on next session -->
| Timestamp | Agent | Summary |
|-----------|-------|---------|
| 2026-05-01 | mounting-framework | Mounted Loa v1.108.0 as submodule; State Zone initialized; CLAUDE.md gained `@.claude/loa/CLAUDE.loa.md` import; build-environment-substrate-v07a1.md ported into grimoires/loa/specs/ |
| 2026-05-02 | sprint-planner | Initialized cycle-001 ledger; authored sprint.md decomposing spec Phases B-E (required) + F (optional) into 5 sprints; created 31 beads (5 epics + 26 tasks) with full dependency graph; auto-assigned 5 cycle goals (G-1..G-5) from spec invariants/done-bar; 5 ready issues at start-line (Sprint 1's first tasks + Sprint 5 epic) |
| 2026-05-02 | session-09-close | imagegen webhook attachment fix landed (PR #15 → main `80cf6e4`) → v0.11.3 tagged + released. Bedrock returns `imageBase64 + filename + mimeType` when `placeholder=false`; new `sendImageReplyViaWebhook` decodes Buffer → discord.js `AttachmentBuilder` → `webhook.send({files: [...]})`. Operator dev-guild verified inline render. Issue #14 closed. WITNESS QA Surface 5 (imagegen render) now green; Surface 7 (gumi blind-judge voice fidelity) remains operator-bounded async. Session 09 trajectory: tool-call faking bug spanned 5 versions (v0.9.1 → v0.11.3); root-cause sequence was provider routing (v0.11.1) → assistant-text fallback for Bedrock (v0.11.2) → bytes-actually-attached for imagegen (v0.11.3). |
| 2026-05-02 | session-03-mcp-orgkickoff-audit | Re-confirmed §0.5 audit findings of `freeside-mcp-organization-kickoff-2026-05-01.md` against current code. All 5 drift gaps hold. Deltas flagged: (1) Effect 3.x consolidates Schema into `effect ^3.21.2` — no separate `@effect/schema` (gateway pin), (2) chat-mode routing infra (Phase D) shipped 2026-05-02; this session adds REGRESSION TESTS + schema not bootstrap, (3) ruggy/persona.md:881 references `mcp__rosenzu__threshold` — drift candidate, will surface concretely via §4.3c persona-tool-drift test. Beads cycle-001 (V0.7-A.1 env substrate) stays focused; this MCP-organization work runs parallel under MAY-LATITUDE per seed §9.6, tracked via TaskCreate session display. |
| 2026-05-02 | session-03-mcp-orgkickoff-close | All §4 deliverables landed. §4.1-§4.4 in-repo · §4.5/§4.6 drafted as proposals/ awaiting operator coordination push. 49 tests pass (24 boundary contract · 16 surface-completeness · 9 persona-tool-drift) · full typecheck green. Real drift caught + fixed by §4.3c on first run: satoshi/persona.md prompted score/rosenzu/freeside_auth tools that satoshi.character.json mcps[] excluded — orchestrator was filtering them out at runtime, broken-chat class for satoshi. Resolution: expanded satoshi.mcps[] to ["score","codex","rosenzu","freeside_auth","imagegen"] matching what persona explicitly assumed ("same substrate as ruggy"). Surfaced as deviation per feedback_spec_deviation_pattern; operator can revert + alternative-fix the persona refactor if intent was narrow scope. Effect.Schema substrate now load-bearing: emojis migration is the worked example; pattern travels to rosenzu/freeside_auth/imagegen in follow-up sessions. ruggy/persona.md:881 `mcp__rosenzu__threshold` reference verified as NOT drift (rosenzu/server.ts registers `threshold` tool · was a false positive in audit). |
| 2026-05-02 | session-03-bridgebuilder-fixup | PR #18 merged accidentally pre-review-fixes; applied F1+F2+F5+F12 directly to main (commit `9cee5bf`). F12 closes the schema↔handler boundary by deriving handler args from `Schema.Schema.Type<typeof Input>` — the architectural meditation Bridgebuilder named ("schema is not the contract; the boundary IS the contract"). Three boundaries — schema, Zod runtime, handler args — now share one source of truth. F2 date-stamps `EXTERNAL_TOOLS` allowlist + documents snapshot-test directional bias (consumer drift vs producer drift). F1 removes unused `decodeInput`/`decodeOutput` (parallel-validators-create-incidents per AWS Smithy postmortem). F5 replaces hardcoded counts with registry-derived values. WITNESS QA checklist authored at `grimoires/loa/qa/qa-cycle-session-03-mcp-substrate-2026-05-02.md` for post-merge validation (7 surfaces · 4 dev-guild runtime · 3 async coordination). |
| 2026-05-02 | session-03-vault-ingest | New vault doctrine: `~/vault/wiki/concepts/schema-is-not-the-contract.md` (aliases: boundary-as-contract · effect-vs-zod-runtime-encapsulation · types-lie-unless-something-runs). Captures the precise design-philosophy split: both Zod's `.parse()` and Effect's `Schema.decodeUnknownSync` are opt-in at the call site IN ISOLATION; the difference is structural (Effect schemas compose into `Effect<A,E,R>` values where the runtime traverses the graph automatically · validation falls out of pipeline construction · "encapsulated runtime" emerges from composition not from compiler enforcement). For freeside-characters today: hybrid usage (Effect.Schema as Zod replacement) means we have type-system benefits but not full composition benefits. Aspirational angle: as more layers compose into Effects (HTTP routes via Effect/Platform · federation broadcast · DB queries) the principle materializes structurally. Vault log updated. |
| 2026-05-02 | session-V07A3-kickoff | Cycle-002 (V0.7-A.3) opened. Spec staged at `grimoires/loa/specs/build-env-aware-image-composition-v07a3.md` (copy of bonfire kickoff brief). Sprint plan at `grimoires/loa/sprint-v07a3.md` (single sprint, 3 phases, 10 tasks: §1-§10 image composition + §11 anti-hallucination + WITNESS regression). Beads epic `bd-ypi` + tasks 1-10. Inheritance: G1 gate green via PR #15 V0.11.3 imagegen webhook attachment shipped (commit `80cf6e4`); pattern is the architectural mirror for grail-image-from-codex. Author: V0.7-A.3 kickoff via bonfire session, operator override "Do this full /autonomous". Skill tool resolves from bonfire CWD so freeside-characters work executes via Agent dispatch with absolute paths. Doctrine candidate validated by this cycle: `~/vault/wiki/concepts/environment-aware-composition.md` (third mutability layer beyond [[metadata-as-integration-contract]]). |
| 2026-05-02 | session-04-expression-layer-close | All §4 deliverables landed (§4.0–§4.5). 134 tests pass (33 new in expression layer + provider-resolution matrix · 0 regressions in PR #18 surface-completeness/persona-tool-drift) · full typecheck green. **§4.0 BEDROCK FLIP**: `agent-gateway.ts:resolveProvider` + `reply.ts:resolveChatProvider` auto-rule both prefer bedrock-when-AWS-env-present → anthropic (dev) → stub → freeside. Boot log reads `llm: bedrock-direct (LLM_PROVIDER=auto resolved · ...)`. Production already mitigated via ANTHROPIC_API_KEY removal; §4.0 codifies the rule. **§4.1**: `expression/tool-mood-map.ts` authored with Effect.Schema · 6-MCP coverage. **§4.2**: `dispatch.ts onToolUse` rewired through `composeToolUseStatusForCharacter` — ruggy gets animated emoji from his catalog, satoshi gets locked plain-text "the ledger advances — one moment.". **§4.3**: `expression/error-register.ts` per-character per-error-class templates · ruggy lowercase/slangy · satoshi sentence-case/gnomic · strip-the-name test passes. **§4.4**: `apps/character-ruggy/silence-register.md` (6 templates) + `expression/silence-register.ts` + composer.ts wired to bypass LLM on flat windows (events<100 + no climbed + no spotlight). **§4.5**: `docs/EXPRESSION-TIMING.md` authored · `MIN_TOOL_PATCH_INTERVAL_MS` bumped 500→800ms per YANAGI cadence. **§4.6 KEEPER deferred** to V1.5 per `feedback_character_play_vs_friction` discipline (listen first, project second). |
| 2026-05-02 | session-V07A3-impl-close | All 3 phases shipped via 3 commits on `feat/v07a3-env-aware-image-composition` → DRAFT PR [#21](https://github.com/0xHoneyJar/freeside-characters/pull/21). Phase A (`1c4c5a2`): `composeWithImage` + `composeReplyWithEnrichment` (additive sibling per spec §4.1 option C) + orchestrator `tool_result` capture + dispatch wire. Phase B (`4fb6698`): `grail-ref-guard` validator (V1 hardcoded 7-id canonical set + sessionRefs override + warning footer) + persona CODEX GROUNDING sections in both ruggy + satoshi. Phase C (`c9cebc4`): WITNESS QA at `grimoires/loa/qa/qa-cycle-V07A3-2026-05-02.md` (6 verification surfaces + 3 showcase scenarios). **Tests**: 157/157 pass (111 persona-engine + 46 bot · 23 NEW V0.7-A.3 tests · zero regressions in prior 134) · 553 expects · typecheck green across workspace. **Beads**: bd-ypi.1-10 all closed. **Deviations**: (1) used global `fetch` instead of `undici` (no new dep, Bun ships undici-based fetch); (2) canonical 43 grail IDs hardcoded as 7-id subset (876, 4488, 235, 6458, 4221, 1606, 6805 — operator-attested from spec/dogfood) with V1.5 roadmap to fetch from `mcp__codex__list_archetypes`; (3) gumi voice-authority sync queued (persona changes register-matched but bypass canonical authoring authority). **Operator-bounded**: G-6 KEEPER pass on 2nd server with `assets.0xhoneyjar.xyz` blocklisted = the doctrine validation gate for `~/vault/wiki/concepts/environment-aware-composition.md`. |
| 2026-05-02 | session-V07A3-bridgebuilder-triage | Bridgebuilder findings on PR #21 addressed in 2 commits on `feat/v07a3-env-aware-image-composition`. **F5 + F6 (`b85ec12`) — `fix(deliver): SSRF allowlist + size cap on image fetch`**: closes the HIGH-severity SSRF in `fetchAttachment` (no scheme/host check on tool-result URLs → file:/data:/internal-IP exfil via webhook) with `isAllowedImageUrl` allowlist (`https:` + `assets.0xhoneyjar.xyz`) + `redirect: 'error'` on fetch + `MAX_ATTACHMENT_BYTES = 8MB` cap (Content-Length pre-check + post-arrayBuffer belt-and-braces). 10 new tests. **F1 + F3 + F4 (`bc06c1f`) — `fix(deliver): grail-ref-guard tighten pattern + remove user-visible footer`**: drops the `#<id>` validator pattern (false-positives on issue refs/anchors/ordinals — `@g` prefix is the canonical disambiguator) + removes `GRAIL_REF_GUARD_FOOTER` from user-visible Discord text (broke persona illusion · engineering jargon leaked: KEEPER/V1.5/corpus signal). `appendGrailRefFooter` renamed to `inspectGrailRefs` to make the contract explicit; validation is now operator-only telemetry via `console.warn` + structured `grailRefValidation` field. Persona instructions in both ruggy + satoshi updated to require `@g<id>` form explicitly + forbid bare `#876`. F3's 7-of-43 conservative set kept — with footer removed, false positives are silent corpus-expansion signal not user-visible noise. **Tests**: 174/174 pass (128 persona-engine + 46 bot · 17 net new vs baseline post-V0.7-A.3) · typecheck green. **Deferred**: F2 (session-ref regex asymmetry · V1.5 cleanup), F7 (Content-Type from URL ext · low impact), F8 (PATCH fallback drops attachments · DESIGN-intent per spec §2 graceful-degrade), F9 (search_codex top-1 ordering · depends on codex MCP contract), F10 (footer chunking moot post-F4), F11 (non-text content blocks dropped · log addition sufficient). PR #21 marked ready for review post-triage. |

## Decision Log
<!-- Major decisions with rationale -->

| Date | Decision | Rationale | Decided By |
|------|----------|-----------|------------|
| 2026-05-01 | Submodule mode mount (not checkout) pinned to v1.108.0 | Matches bonfire's pattern; sharing one Loa version across operator repos avoids drift; pin allows controlled bumps via `git submodule update --remote .loa` | operator + ARCH (Ostrom) |
| 2026-05-01 | Phase A ships as standalone PR; substrate work (Phases B-E) deferred to follow-up sessions inside this repo | Clean workflow boundary — once Loa is mounted, B-E run through `/sprint-plan` + `/implement` gates with beads tracking. Avoids "while-I'm-here" scope creep on a framework-mount PR (Barth scope discipline) | operator + SHIP (Barth) |
| 2026-05-02 | Treat the kickoff brief as combined PRD+SDD instead of running /plan-and-analyze + /architect anew | Spec frontmatter declares `external_work_plan: true`; spec already contains invariants, blast radius, component specs, build sequence, and verify-gates. Synthesizing a separate PRD/SDD would dilute the operator's authored brief without adding signal. Sprint plan cites spec lines directly | sprint-planner |
| 2026-05-02 | Sprint = phase 1:1 (5 sprints not bundled) | Each phase has independent revert boundary per spec invariant lines 113-114; bundling Phase B+C or D+E would dilute "stop at phase boundary if voice fidelity regresses" gate from spec line 567 | sprint-planner |
| 2026-05-02 | Phase F kept as Sprint 5 OPTIONAL (not deleted) | Spec explicitly notes operator-approval-gated; promoting it to OPTIONAL keeps the bead-tracked surface visible without blocking cycle completion. Excluding it would lose discoverability of the empirical-density task | sprint-planner |
| 2026-05-02 | Auto-assigned goal IDs G-1..G-5 from spec invariants/done-bar (lines 78-86, 569, 609) | Spec phrases done-bar as paragraph without explicit IDs; protocol requires explicit IDs; assignment logged to trajectory for traceability | sprint-planner |
| 2026-05-02 | `buildEnvironmentContext` takes resolved `zone: ZoneId \| undefined` instead of spec-shown `channelId + config` | Spec's `compose/environment.ts` example called `getZoneForChannel(args.config, args.channelId)` internally, but Sprint 1 placed `getZoneForChannel` in `apps/bot/src/lib/channel-zone-map.ts` (workspace direction prevents persona-engine from importing apps/bot). Bot dispatcher (Sprint 3 / Phase D) owns the channelId→zone resolution; environment.ts gets the resolved zone. Output block shape (the integration contract for the LLM) is unchanged. | sprint-2-implementer |
| 2026-05-02 | Sprint 3 ACs 3.1 + 3.2 (live LLM tool invocation) marked `⏸ [ACCEPTED-DEFERRED]` to Sprint 4 E2E validation | These ACs require a dev-guild deployment with real keys (ANTHROPIC_API_KEY + MCP_KEY + CODEX_MCP_URL) and a live Discord interaction round-trip. The structural pieces are implemented + smoke-verified (CHAT_MODE routing, per-character MCP isolation, environment-context substitution, dispatcher zone-resolution). Sprint 4's E2E matrix (per spec verify gate line 563-564) exercises the live tool invocation alongside persona-prose validation. Deferral preserves the spec-mandated validation while keeping Sprint 3 structurally complete. | sprint-3-implementer |
| 2026-05-02 | `CHAT_MODE` defaults to `auto` (orchestrator if anthropic, naive otherwise), not `orchestrator` as spec line 441 suggested | The orchestrator path is anthropic-SDK-only. Defaulting to `orchestrator` would break bedrock + freeside chat deployments (e.g., satoshi's V0.8.0 Bedrock pipeline). `auto` opt-in for anthropic users + safe fallback for others matches the operator's actual chat-mode topology. Spec assumed anthropic-only chat path; reality is multi-provider. `CHAT_MODE=orchestrator` remains available as an explicit override; `CHAT_MODE=naive` is the revert hatch per spec. | sprint-3-implementer |
| 2026-05-02 | V0.7-A.2 fragment lift: `<!-- @FRAGMENT: reply -->` content is byte-identical between ruggy and satoshi persona.md (49 lines each, copy of historical CONVERSATION_MODE_OVERRIDE) | Bridgebuilder F15 (PR #8 review) flagged that duplicating the same content per character anticipates per-character variation that doesn't yet exist. Decision: ACCEPT THE DUPLICATION. Rationale: (1) Per-character voice tuning is the long-term direction — satoshi's gnomic register may want different chat-mode framing than ruggy's festival-NPC register; the lift creates the seam where divergence can land without a substrate refactor. (2) The previous shared constant was a single point of churn — every chat-shape iteration touched substrate code. (3) `[[continuous-metadata-as-daemon-substrate]]` doctrine treats persona.md as L2 metadata: per-character + operator-mutable + clean integration contract. (4) If divergence never lands, the duplication is harmless prose; if it does, this commit is the prerequisite. Eileen async review will confirm the byte-identical content during ratification. | sprint-V07A2-implementer |
| 2026-05-02 | Imagegen delivery uses image-as-artifact, not URL-as-utterance: caption shows `> @user: prompt\n_character · model · seed_` and the image renders inline as a multipart attachment; the previous `attachment://...` text-content approach is gone | Codex-rescue cross-model review (PR #15) confirmed: a user can't usefully click an `attachment://` URL — Discord can't resolve it without a multipart `files[]` part in the same webhook send. The artifact IS the image; metadata belongs in the caption surface. `bedrock-client.ts:99` still returns the pseudo-URL as a sentinel for placeholder-mode compatibility (Eileen's PR not yet landed) but the dispatcher checks `placeholder=false && imageBase64 && filename` first and routes to bytes path. | session-09-close |
| 2026-05-02 | MCP-organization session 03 treats `freeside-mcp-organization-kickoff-2026-05-01.md` as the combined PRD+SDD+plan; runs under MAY-LATITUDE per seed §9.6 + 2026-05-02 sprint-planner precedent. No new beads cycle opened. | Seed has all PRD+SDD+sprint-plan shape (§0-§9); /implement gate "may not apply cleanly" is acknowledged in seed §9.6; substrate strengthening is parallel to V0.7-A.1 product-feature work in cycle-001 (different blast radius, different verify-gate). Avoiding cycle-002 prevents two simultaneous cycles competing for the same operator attention; TaskCreate session display + NOTES.md log is the right granularity per Loa task-tracking hierarchy. | session-03-mcp-orgkickoff |
| 2026-05-02 | Effect.Schema substrate uses `import { Schema } from "effect"` (single package) instead of seed §4.1's `effect + @effect/schema` two-package recommendation | Effect 3.x consolidated `Schema` into the main `effect` package; the separate `@effect/schema` package is legacy. Gateway (`0xHoneyJar/freeside-mcp-gateway/package.json` v0.1.0) pins `effect ^3.21.2` only — alignment dictates we follow that pin. Seed §9.5 named "align with gateway" as a coordination requirement; the consolidated-package shape is the load-bearing alignment. | session-03-mcp-orgkickoff |
| 2026-05-02 | Effect contract pattern uses Zod parity-shape alongside Effect.Schema rather than a runtime Effect→Zod converter | The Anthropic Claude Agent SDK's `tool()` registration accepts a ZodRawShape for input validation — not arbitrary schemas. Three options considered: (a) write Effect→Zod AST converter [~80-120 lines, fragile], (b) duplicate schemas in both formats [no source of truth], (c) declare both shapes side-by-side in schema.ts with `assertZodParity<T>()(zodShape)` enforcing TypeScript-level structural equivalence. (c) chosen: drift fails compilation, no AST walking, the Effect contract IS the canonical shape and the Zod is a typed render of that contract. Tests use Effect.decodeUnknownSync at the boundary (defense-in-depth + proves parity). | session-03-mcp-orgkickoff-impl |
| 2026-05-02 | satoshi.character.json mcps[] expanded from `["codex","imagegen"]` to `["score","codex","rosenzu","freeside_auth","imagegen"]` | §4.3c persona-tool-drift test surfaced real drift on first run: satoshi/persona.md (lines 321/325-326/330/335/540) explicitly prompts the LLM to call score/rosenzu/freeside_auth tools, but satoshi's allowlist was narrowed to codex+imagegen — orchestrator filters out the rest, model fabricates responses for filtered-out tools (broken-chat class for satoshi). Persona explicitly says "you compose by calling tools — same substrate as ruggy" so persona-as-canonical implies satoshi needs full substrate access. Fix matches that. tool_invocation_style updated to mention all 5 tools. Reversibility: if operator's intent was narrow-scope satoshi (and persona.md was vestigial), the alternative fix is a persona refactor — bigger creative-direction call needing bonfire grimoires sync first. Surfaced per feedback_spec_deviation_pattern. | session-03-mcp-orgkickoff-impl |
| 2026-05-02 | Cross-repo deliverables (§4.5 construct-freeside skill · §4.6 capability-request templates on score-mibera + construct-mibera-codex) drafted in `grimoires/loa/proposals/` rather than pushed | Per seed §9.2-9.4 escalations: zerker, gumi, and operator (construct-freeside owner) coordination expected before opening cross-repo PRs. Drafts allow operator to review content + push when coordination ready. Content cited where it would land; reshaping for each repo's local conventions noted in draft authoring-notes. | session-03-mcp-orgkickoff-impl |
| 2026-05-02 | Tool-mood vocabulary deviates from kickoff §3 metaphor moods (`digging` · `pattern-watching` · `lore-checking` · `reading-the-room` · `who-is-this`) → use existing registry register-moods (`eyes` · `flex` · `noted` · `cool` · `dapper` · `peace` · `wave`) | Seed §9.3 named the metaphor moods as "MUST exist in the emojis MCP registry OR be added via capability-request." Audit confirmed registry has 27 register-moods, none of which are the seed's metaphor names. Two options: (a) extend registry with metaphor moods (capability-request to PR #18 substrate · cross-repo coordination · expand scope), (b) map tool categories to existing register moods that read in voice for each category (no scope expansion · ships V1 today). (b) chosen per BARTH SHIP discipline. Capability-request to extend mood vocabulary is a follow-up if KEEPER pass surfaces register-fit problems. NOTES.md tracks the deviation; `tool-mood-map.ts` module docstring carries the trail. | session-04-expression-layer |
| 2026-05-02 | Loading-status emoji selection unions the WHOLE mood pool from a tool's mapping rather than picking one mood at random THEN filtering | Single-mood-pick (kickoff §3 wording) silently fails when a mood has zero ruggy-kind emojis. Audit example: registry's `noted` mood has 1 emoji (mibera-kind only); a `mcp__codex__*` call with `pickRandomMood(['noted','eyes'])` landing on `noted` would return null and skip the patch — operator-named goal was every mapped tool emits an animated emoji for ruggy. Fix: `pickByMoods([...moodPool], affinity)` unions ALL pool moods into one candidate set, then random-picks. Variance preserved (4-7 emojis per category union for ruggy); patch reliability achieved. Test added that confirms variance > 1 emoji over 30 fires per tool. | session-04-expression-layer |
| 2026-05-02 | §4.4 silence-register wiring split: substrate composer bypass landed in this session · ruggy persona.md cross-reference deferred to gumi sync | Ruggy's silence templates (`apps/character-ruggy/silence-register.md` · 6 voice-locked templates) introduce voice content the gumi-locked persona doesn't yet hold. Per kickoff §9.2: "operator may want to draft templates → gumi review → merge." Substrate-side bypass works regardless of persona.md (composer detects flat window deterministically + picks template directly). Persona.md cross-reference (telling the LLM about the silence register) is a separate edit that needs gumi's voice authority before landing. V1 ships substrate-bypass; persona.md update queued. | session-04-expression-layer |
| 2026-05-02 | `MIN_TOOL_PATCH_INTERVAL_MS` raised 500ms → 800ms per kickoff §4.5 YANAGI cadence | The 500ms gate was V0.7-A.1's first-cut — F4 PR #8 surfaced rapid-tool-chain spam at 500ms but didn't tune for perceptual cadence. YANAGI (kansei persona) framing: < 800ms reads as flicker/jitter; 800ms-4s reads as alive/present; > 4s reads as abandoned. The bump moves the floor into the alive zone. Documented in `docs/EXPRESSION-TIMING.md`. KEEPER deferred pass (V1.5) confirms or amends via real user reactions. Reversibility: trivial constant. | session-04-expression-layer |
| 2026-05-02 | `tool-verb.ts` deleted · was unused after `dispatch.ts onToolUse` switched to `composeToolUseStatusForCharacter` | Pre-V0.12 `toolVerb()` returned generic `🔧 [verb]…` strings used by the old onToolUse PATCH. New path resolves through the substrate-side mood map; no caller remains. Per session policy "avoid backwards-compatibility hacks like ... // removed comments for removed code", clean deletion is preferable to leaving an unused module. Comments in `dispatch.ts` retain a one-line reference to the prior shape for archaeological context. | session-04-expression-layer |
| 2026-05-02 | `resolveProvider` exported from `agent-gateway.ts` · added `provider-resolution.test.ts` table-driven matrix | Per kickoff §4.0 verify-gate: "provider-resolution test table green." The function was previously file-local; testing the digest-path auto-rule required either exporting it or testing via behavior (calling `invoke()` and observing routing). Direct export is the legible option · parallels `resolveChatProvider`'s already-exported shape (reply.ts). Both resolvers now share a 7-row table-driven test that locks the bedrock-first preference order against regression. | session-04-expression-layer |
| 2026-05-02 | F1 grail-ref pattern → drop `#<id>` form entirely; only `@g<id>` is recognized | Bridgebuilder F1 flagged `/#(\d+)/g` as false-positive prone (issue refs `#123`, markdown anchors, channel mentions, ordinal lists). Tightening with min-3-digit + word-boundary still ambiguates against PR refs. Pragmatic resolution: the `@g` prefix IS the disambiguator that separates a grail ref from any other `#`-token. Persona instructions updated in both characters to forbid bare `#876`. Reversibility: trivial (re-add `REF_HASH_PATTERN`); but doing so re-opens the false-positive class. | session-V07A3-bridgebuilder-triage |
| 2026-05-02 | F4 grail-ref-guard footer removed → V1 validator becomes operator-only telemetry | Bridgebuilder F4 flagged `GRAIL_REF_GUARD_FOOTER` ("[KEEPER: contains unverified ref(s) · captured for V1.5 corpus signal]") as engineering-jargon leak into user-facing Discord text — broke persona illusion. Combined with F3's 7-of-43 conservative canonical set, the footer fired frequently on legitimate citations. Decision: text returned UNCHANGED. Validation surfaces via existing `console.warn` at the call site + structured `grailRefValidation` field on `EnrichedReplyResult` for downstream analytics. V1.5 may add an in-voice signal (character refusing the reach) per spec §11.2 — never engineering metadata. F3 unchanged: 7-id set IS the conservative default; with footer gone false positives are silent corpus-expansion signal. `appendGrailRefGuardFooter` renamed → `inspectGrailRefs` to make the contract explicit. | session-V07A3-bridgebuilder-triage |
| 2026-05-02 | F5 + F6 SSRF + size cap defense in `embed-with-image.ts`: scheme/host allowlist + `redirect: 'error'` + `MAX_ATTACHMENT_BYTES = 8MB` | Bridgebuilder F5 (HIGH security): `fetchAttachment` had no URL validation before `fetch(url)` — a misbehaving codex MCP could return `file://`, `data:`, link-local IP `169.254.169.254` (cloud metadata), `http://localhost:*`, or `https://attacker` redirecting to those targets. Bytes attached to Discord webhook = exfiltration channel. F6 (MEDIUM security): `arrayBuffer()` had no size cap; multi-GB stream could exhaust memory before the 5s timeout fires. Mitigations chosen: hard-coded allowlist (`ALLOWED_SCHEMES = {'https:'}`, `ALLOWED_IMAGE_HOSTS = {'assets.0xhoneyjar.xyz'}`) + `redirect: 'error'` (undici throws on any 3xx) + 8MB cap (well under Discord 25MB single-attachment limit; pre-fetch Content-Length check + post-arrayBuffer belt-and-braces). V1.5 follow-up: load allowlist from config/env so adding `metadata.0xhoneyjar.xyz` or per-tenant CDNs doesn't require a code change. Reversibility: revert single commit; allowlist is opt-in widening, not opt-out narrowing. | session-V07A3-bridgebuilder-triage |
| 2026-05-02 | V0.7-A.3 HOTFIX B1 — `search_codex` envelope shape: `SearchHit[]` direct-array + scan-all-results | PR #21 implementation expected `{ results: [{...}] }` envelope shape from `mcp__codex__search_codex` per spec §2.2. Operator's "the dark grail" Discord test surfaced silent-drop in prod. Verified against codex MCP source at `/tmp/codex-probe/src/lookups/search.ts:204` (`export function searchCodex(opts: SearchOptions): SearchHit[]`) + `server.ts` handler returning `{content: [{type: "text", text: JSON.stringify(hits, null, 2)}]}` — no wrapper object. Two corrections in `pickFirstGrailFromEnvelope`: (1) parse `SearchHit[]` directly when `toolName === search_codex`, (2) scan all hits in order until one with image field surfaces (top-1 may be non-grail core-lore hit; bridgebuilder F9 deferred lands here). Telemetry: warn when `search_codex returned N hits but none had image field` so operators diagnose envelope-shape regression vs non-grail-only result sets. Tests: 12 envelope-shape cases including legacy `{results:[...]}` shape returns null (defensive) + lookup_grail flat + nested regression (PR #21 path preserved). | session-V07A3-hotfix |
| 2026-05-02 | V0.7-A.3 HOTFIX B2 — dual-layer URL strip: composer-side `stripAttachedImageUrls` (B2a) + persona "never paste image URLs" instruction (B2b) | Operator runtime test 2026-05-02 PM had `attached=1` (bytes attached via webhook · env-aware composer fired correctly) but the Discord message DISAPPEARED. Root cause: LLM saw `image: 'https://assets.0xhoneyjar.xyz/.../black-hole.png'` in the lookup_grail tool result envelope and naturally pasted that URL inline in voice prose. Discord automod still triggered on the URL — bytes-on-the-wire contract defeated by URL-paste residual. Single-layer fix options considered: (a) persona-only — fragile (LLM ignores rule under load · OG/festival register may rationalize URL as in-character "show the link"), (b) composer-only strip — silent fix, no learning signal back to LLM. Both layers shipped: persona instruction (B2b) addresses the LLM-side habit per character register (ruggy lowercase: "ngl that's not the vibe" / satoshi sentence-case: "the channel's filters are also part of the protocol"); composer-side strip (B2a) is the belt-and-braces that catches LLM regression silently with `[embed-with-image] stripped N inline image URL(s)` telemetry so operators see when persona-side failed and the safety net caught it. Defense in depth pattern; either alone is fragile. Strip handles markdown image / markdown link / plain URL paste; rechunks stripped text via `splitForDiscord` (boundaries shift); only strips URLs in the actual `payload.files` set so non-attached references survive. Reversibility: revert is mechanical. | session-V07A3-hotfix |
| 2026-05-02 | V0.7-A.3 polish PR — F10+F4+F3+F8 addressed; F5+F6+F7 deferred as tunables | Bridgebuilder findings from PR #22 follow-up triage: (F10 MEDIUM) `composeReplyWithEnrichment` mutated `payload.content` post-return + used `extractAttachedUrls(grailCandidates).slice(...)` workaround whose ordering invariant was the F4 LOW correctness smell. Single fix: extend `EnrichedPayload` with `attachedUrls` populated by composeWithImage from successful fetches only (FetchedAttachment {file, sourceUrl} pairing). (F3 LOW) markdown link form was dropping visible noun phrases — link text now PRESERVED as plain prose; image alt-text continues to drop entirely (alt is invisible vs link text is voice). (F8 LOW) dead `imageBearing` counter removed from `pickFirstGrailFromEnvelope` — emit warning unconditionally post-loop. Deferred as tunables (inline comments cite the F# in source): F5 (whitespace punctuation regex global-by-design — benign normalization), F6 (URL query-string handling — codex returns canonical paths, defensive belt-and-braces), F7 (telemetry warning wording — diagnostic signal, not user-facing). Composer is character-agnostic per OSTROM invariant 2: operator dogfood post PR #22 confirmed both ruggy + satoshi work for "the dark grail" + "show me grail #876" — wider validation than spec's V1=satoshi-only scope. 162/162 persona-engine tests · 46/46 bot tests · typecheck green. | session-V07A3-polish |
