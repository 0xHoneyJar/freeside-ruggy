{
  "name": "@freeside-characters/protocol",
  "version": "0.8.0",
  "private": true,
  "description": "Placeholder for sealed-schema package. Each character is a score-vault consumer; this package may grow if characters publish schemas (ChannelConfig, DigestPostMeta, etc.).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
